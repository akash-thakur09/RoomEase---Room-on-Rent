/**
 * propertyModel.js — NEW (replaces roomModel.js)
 * Re-exports from modules/property/model.js.
 * The Mongoose model name remains 'Room' for backward compatibility
 * with existing documents in the 'rooms' collection.
 */
module.exports = require('../modules/property/model');
