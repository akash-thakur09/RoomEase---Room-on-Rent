/**
 * bookingModel.js — NEW (replaces requestModel.js)
 * Re-exports from modules/booking/model.js.
 * Replaces the old Request model with a richer Booking schema.
 */
module.exports = require('../modules/booking/model');
